/*
 * Local variables:
 *   c-file-style: "parrot"
 * End:
 * vim: expandtab shiftwidth=4:
 */
pmclass PhpArray
    dynpmc
    need_ext
    auto_attrs
    hll      PHP
    group    pipp_group
    provides hash
	extends  OrderedHash
    maps     OrderedHash {

    ATTR struct pipp_hash_table *hash_table;

	void class_init() {}
	
	void init() {
		SUPER();
	};
	
	void set_pmc(PARROT_INTERP, ARGMOD(PMC *_self), PMC *value)) {
		SELF.set_pmc(value);
	}
}
